procs:
  client:
    cmd:
      - cargo
      - run
      - --bin
      - client
      - --
  client2:
    cmd:
      - cargo
      - run
      - --bin
      - client
      - --
      - --hotkey
      - Pause
  server:
    autostart: false
    cmd:
      - cargo
      - run
      - --bin
      - server

hide_keymap_window: true
